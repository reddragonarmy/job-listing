<div class="col-md-12">
  <div class="group">
    <%= link_to("投简历", new_job_resume_path(@job), class: "btn btn-default pull-right")%>
  </div>

  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>

</div>